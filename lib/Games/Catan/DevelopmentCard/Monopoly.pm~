package Games::Catan::DevelopmentCard::Monopoly;

use Moo;

with( 'Games::Catan::DevelopmentCard' );

has num_points => ( is => 'ro',
                    isa => Int,
                    required => 0,
                    default => 1 );

1;
